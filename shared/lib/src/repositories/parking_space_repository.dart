import 'base_repository.dart';
import '../models/parking_space.dart';

abstract class ParkingSpaceRepository extends BaseRepository<ParkingSpace> {
  // You can add additional methods specific to ParkingSpaceRepository here if needed.
}