import 'base_repository.dart';
import '../models/person.dart';

abstract class PersonRepository extends BaseRepository<Person> {
  // You can add additional methods specific to PersonRepository here if needed.
}