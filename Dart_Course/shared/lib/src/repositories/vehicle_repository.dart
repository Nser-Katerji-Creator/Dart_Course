import 'base_repository.dart';
import '../models/vehicle.dart';

abstract class VehicleRepository extends BaseRepository<Vehicle> {
  // You can add additional methods specific to VehicleRepository here if needed.
}