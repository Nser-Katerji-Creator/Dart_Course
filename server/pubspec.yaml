name: server
description: A server app using the shelf package and Docker.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.0

dependencies:
  http: ^1.3.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^2.0.0  # Use sqlite3 instead of sqflite
  path: ^1.8.0
  shared:
    path: ../shared

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0